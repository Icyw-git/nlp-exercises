model:
  dim: 512
  n_layers: 8
  n_heads: 8
  vocab_size: 51200
  max_seq_len: 512

data:
  train_path: ./data/input.txt
  max_length: 512

training:
  learning_rate: 3e-4
  batch_size: 4
  epochs: 3
  save_steps: 100
  output_dir: ./checkpoints/pretrain
